// ファイル責務: デッキ読み込み完了を通知するイベント名を一元管理し、文字列の書き間違いを防ぐ。
// 実行ロジックは持たず、イベント名を定数として公開するだけで他モジュールが共通参照できるようにする。

/**
 * ボタン側から「デッキが読み込まれた」と通知するためのイベント名。
 * 複数箇所に同じ文字列を書くとミスを招くため、ここで定数化して共通で利用する。
 */
export const DECK_IMPORTED_EVENT = "deck:imported";
